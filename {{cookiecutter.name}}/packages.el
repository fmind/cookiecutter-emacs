;; package configurations
;; use use-package to speed-up the loading
;; for each package, you may define the following functions:
;; - <layer>/init-<package>
;; - <layer>/pre-init-<package>
;; - <layer>/post-init-<package>

(defconst {{cookiecutter.name}}-packages '())
